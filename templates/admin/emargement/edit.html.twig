{% extends 'admin/Dashboard/administration1.html.twig' %}

{% block title %}Emargement
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-lg-2">
			<a href="{{ path('app_emargement_index') }}">
				<i class="glyphicon glyphicon-chevron-left"></i>
				<span>Retour
				</span>
			</a>
		</div>
		<div class="col-lg-1">
			{{ include('admin/emargement/_delete_form.html.twig') }}
		</div>
	</div><br>
	<section class="content-header">
		<h1>
			Modification de l'émargement
			<small>Formulaire</small>
		</h1>
	</section>
	{{ include('admin/emargement/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
