{% extends 'base.html.twig' %}

{% block title %}Devoir
{% endblock %}

{% block body %}
	<h1>Devoir</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ devoir.id }}</td>
			</tr>
			<tr>
				<th>DateDevoir</th>
				<td>{{ devoir.DateDevoir ? devoir.DateDevoir|date('Y-m-d') : '' }}</td>
			</tr>
			<tr>
				<th>Note</th>
				<td>{{ devoir.Note }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_devoir_index') }}">back to list</a>

	<a href="{{ path('app_devoir_edit', {'id': devoir.id}) }}">edit</a>

	{{ include('admin/notes/devoir/_delete_form.html.twig') }}
{% endblock %}
