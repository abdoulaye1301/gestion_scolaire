{% extends 'admin/Dashboard/administration1.html.twig' %}

{% block title %}Inscription
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-lg-2">
			<a href="{{ path('app_inscription_index') }}">
				<i class="glyphicon glyphicon-chevron-left"></i>
				<span>Retour
				</span>
			</a>
		</div>
		<div class="col-lg-1">
			{{ include('admin/inscription/_delete_form.html.twig') }}
		</div>
	</div><br>
	<section class="content-header">
		<h1>
			Modification de l'inscription de l'élève
			<small>Formulaire</small>
		</h1>
	</section>

	{{ include('admin/inscription/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
