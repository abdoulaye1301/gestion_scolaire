{% extends 'admin/Dashboard/administration1.html.twig' %}

{% block title %}Modification de l'enseignant
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-lg-2">
			<a href="{{ path('app_professeurs_index') }}">
				<i class="glyphicon glyphicon-chevron-left"></i>
				<span>Retour
				</span>
			</a>
		</div>
		<div class="col-lg-1">
			{{ include('admin/professeurs/_delete_form.html.twig') }}
		</div>
	</div><br>
	<section class="content-header">
		<h1>
			Modification de l'enseignant
			<small>Formulaire</small>
		</h1>
	</section>

	{{ include('admin/professeurs/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
