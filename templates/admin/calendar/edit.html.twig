{% extends 'admin/Dashboard/administration1.html.twig' %}

{% block title %}Modification de l'événement
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-lg-2">
			<a href="{{ path('app_calendar_index') }}">
				<i class="glyphicon glyphicon-chevron-left"></i>
				<span>Retour
				</span>
			</a>
		</div>
		<div class="col-lg-1">
			{{ include('admin/calendar/_delete_form.html.twig') }}
		</div>
	</div>
	<section class="content-header">
		<h1>
			Modification de l'événement
			<small>Formulaire</small>
		</h1>
	</section>
	{{ include('admin/calendar/_form.html.twig', {'button_label': 'Modifier'}) }}


{% endblock %}
