<section class="container my-3" style=" margin-top:10px; margin-left:200px;">
	<div class="row">
		<div class="col-lg-6">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			<button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
			{{ form_end(form) }}
		</div>
	</div>
</section>
