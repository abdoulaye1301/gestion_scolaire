{% extends 'admin/Dashboard/administration1.html.twig' %}

{% block title %}Modification Classe
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-lg-2">
			<a href="{{ path('app_classe_index') }}">
				<i class="glyphicon glyphicon-chevron-left"></i>
				<span>Retour
				</span>
			</a>
		</div>
		<div class="col-lg-1">
			{{ include('admin/classe/_delete_form.html.twig') }}
		</div>
	</div>
	<section class="content-header">
		<h1>
			Modification de la classe
			<small>Formulaire</small>
		</h1>
	</section>
	{{ include('admin/classe/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
