{% extends 'admin/Dashboard/administration1.html.twig' %}

{% block title %}Eleves
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-lg-2">
			<a href="{{ path('app_eleves_index') }}">
				<i class="glyphicon glyphicon-chevron-left"></i>
				<span>Retour
				</span>
			</a>
		</div>
		<div class="col-lg-1">
			{{ include('admin/eleves/_delete_form.html.twig') }}
		</div>
	</div><br>

	{{ include('admin/eleves/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
